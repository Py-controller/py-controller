from django.urls import path
from . import views

urlpatterns = [
    path('reports/', views..as_view()),
    path('reports/plannings/', views..as_view())
    path('reports/accounts/', views..as_view())

]
